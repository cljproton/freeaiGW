services:
  gateway:
    image: ${GHCR_REPO:-ghcr.io/cljproton/freeaigw}:${IMAGE_TAG:-latest}
    container_name: freeai-gateway
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:${PORT:-8791}:8791"
    volumes:
      - ${DATA_DIR:-./data}:/app/data
